name: Perl Lint

on:
  push:
    paths:
      - "**.pl"

jobs:
  perlcritic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: Difegue/action-perlcritic@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: --brutal **/*.pl
  perltidy:
    runs-on: ubuntu-latest
    container:
      image: perl:5.32
    steps:
      - uses: actions/checkout@v2
      - name: perl -V
        run: perl -V
      - name: Install dependencies
        run: cpanm -n Perl::Tidy
      - name: perltidy --version
        run: perltidy --version
      - name: Run perltidy
        shell: bash
        run: |
          shopt -s extglob globstar nullglob
          perltidy --pro=.../.perltidyrc -b -bext='/' **/*.p[lm] **/*.t && git diff --exit-code
  # perl_lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - name: Set up Perl 
  #       uses: shogo82148/actions-setup-perl@v1
  #       with:
  #         perl-version: '5.34'
  #     - name: Install perlcritic
  #       run: cpanm Task::PerlCriticAllPolicies
  #     - name: Run perlcritic
  #       run: perlcritic . --verbose 11
        